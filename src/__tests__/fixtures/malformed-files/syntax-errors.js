// This file contains intentional syntax errors for testing

function incompleteFunction( {
  // Missing closing parenthesis and opening brace

const missingValue = ;
// Missing value after assignment

class MissingBrace {
  constructor() {
    this.value = "test";
  // Missing closing brace

export { something that doesn't exist };

import from 'nowhere';

// Unmatched brackets and braces
function test() {
  const obj = {
    key1: 'value1',
    key2: [1, 2, 3,
    // Missing closing bracket and brace
  };
}

// Invalid characters in identifier
const invalid-variable-name = "test";

// Incomplete string
const incompleteString = "this string never ends

// Multiple syntax errors on one line
function bad() { return }} {{ ;;