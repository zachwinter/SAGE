{
  "name": "@sage/monorepo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -F @sage/cli dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:cli": "pnpm -F @sage/cli test",
    "test:aql": "pnpm -F @wearesage/aql test",
    "test:integration": "pnpm -F @sage/cli test:integration",
    "test:e2e": "pnpm -F @sage/cli test:e2e",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write .",
    "clean": "pnpm -r clean",
    "docs:types": "typedoc --options typedoc.config.cjs contract-types.ts",
    "docs:build": "node scripts/readme.js && pnpm docs:types"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "pnpm": {
    "workspaces": [
      "apps/*",
      "packages/*"
    ]
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.8.0",
    "@types/react": "^19.1.10",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "chalk": "^5.6.0",
    "glob": "^11.0.3",
    "prettier": "^3.6.2",
    "tsx": "^4.20.4",
    "typedoc": "^0.25.2",
    "typedoc-plugin-markdown": "^4.8.1",
    "typescript": "^5.9.2",
    "vite-tsconfig-paths": "^5.1.4"
  },
  "files": [
    "dist/"
  ],
  "dependencies": {
    "axios": "^1.11.0",
    "uuid": "^11.1.0"
  }
}
