{
  "name": "@sage/monorepo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -F @sage/cli dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:cli": "pnpm -F @sage/cli test",
    "test:analysis": "pnpm -F @sage/analysis test",
    "test:integration": "pnpm -F @sage/cli test:integration",
    "test:e2e": "pnpm -F @sage/cli test:e2e",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write .",
    "clean": "pnpm -r clean"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "pnpm": {
    "workspaces": [
      "apps/*",
      "packages/*"
    ]
  },
  "devDependencies": {
    "@types/react": "^19.1.10",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "tsx": "^4.20.4"
  },
  "files": [
    "dist/"
  ]
}
